require('dotenv').config();const express = require('express');const cors = require('cors');const stripe = require('stripe')(process.env.STRIPE_SECRET_KEY);const app = express();app.use(cors({  origin: [    'https://coldtransportrentals.com.au',    'https://www.coldtransportrentals.com.au',    'http://localhost:3001',  ]}));app.use(express.json());const path = require('path');app.use(express.static(__dirname));// ─── Helper: find existing customer by email, or create new ───────────────────async function findOrCreateCustomer({ company_name, email, address_line1, city, postcode, state, country, abn, licence_number, licence_state }) {  const existing = await stripe.customers.list({ email, limit: 1 });  if (existing.data.length > 0) return existing.data[0];  return stripe.customers.create({    name: company_name,    email,    address: { line1: address_line1, city, postal_code: postcode, state, country: country || 'AU' },    metadata: { abn, licence_number, licence_state },  });}// ─── 1. Standard payment OR hold ─────────────────────────────────────────────app.post('/api/create-payment', async (req, res) => {  const { paymentMethodId, hold_only, ...customerData } = req.body;  const { company_name, abn, licence_number, licence_state } = customerData;
